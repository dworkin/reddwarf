This distribution contains the following subdirectories:

- sgs-server-${sgs.version}
  Project Darkstar Server

- bdb-${bdb.version}
  Berkeley DB

See the README file in the sgs-server-${sgs.version} subdirectory for instructions.

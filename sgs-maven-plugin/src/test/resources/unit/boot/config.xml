<project>
  <build>
    <plugins>
      <plugin>
	<groupId>com.projectdarkstar.maven.plugin</groupId>
        <artifactId>sgs-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>install</goal>
              <goal>boot</goal>
	      <goal>stop</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
	  <sgsHome>${basedir}/target/test-classes/unit/boot/sgs-server-dist-${minimum.sgs.version}</sgsHome>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
